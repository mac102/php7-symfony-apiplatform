<table class="table datagrid">
    <thead>
        <tr>
            <th>#</th>
            <th>Author</th>
            <th>Content</th>
        </tr>
    </thead>
    {% for comment in value %}
        <tr>
            <td>{{ comment.id }}</td>
            <td>{{ comment.author.name }}</td>
            <td>{{ comment.content }}</td>
        </tr>
    {% endfor %}
</table>